runtime:
  providers:
    antigravity:
      type: antigravity_acp
      antigravity_acp: {}

knowl:
  provider: antigravity
  workspace:
    path: .
  storage:
    type: sqlite
    sqlite:
      path: .knowl/state.db
  scope: local
  sources:
    - id: engineering-docs
      type: filesystem
      enabled: true
      config:
        filesystem:
          root: sources
          include:
            - "**/*.md"
          flavor: markdown
