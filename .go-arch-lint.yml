version: 1

allow:
  depOnAnyVendor: true

excludeFiles:
  - "^.*_test\\.go$"

components:
  cli:
    in: cmd/knowl
  host:
    in: pkg/knowl
  fx:
    in: pkg/knowlfx
  app:
    in: pkg/knowl/app
  types:
    in: pkg/knowl/types
  wiki:
    in: pkg/knowl/wiki
  content:
    in: pkg/knowl/content/**
  store:
    in: pkg/knowl/store/**
  provider:
    in: pkg/knowl/provider
  mcp:
    in: pkg/knowl/mcp
  mcphttp:
    in: internal/mcphttp
  httpapi:
    in: internal/httpapi/**
  bootstrap:
    in: internal/bootstrap
  testsupport:
    in: pkg/knowl/internal/**
  examples:
    in: examples/**

deps:
  wiki:
    mayDependOn:
      - types
  app:
    mayDependOn:
      - types
      - wiki
  content:
    mayDependOn:
      - app
      - types
      - wiki
  store:
    mayDependOn:
      - store
      - app
      - types
  provider:
    mayDependOn:
      - app
      - types
  mcp:
    mayDependOn:
      - app
      - types
  mcphttp:
    mayDependOn:
      - mcp
  httpapi:
    mayDependOn:
      - httpapi
      - app
      - types
  bootstrap:
    mayDependOn:
      - content
      - types
  host:
    mayDependOn:
      - app
      - types
      - content
      - store
      - provider
      - mcp
      - mcphttp
      - httpapi
  fx:
    mayDependOn:
      - host
  cli:
    mayDependOn:
      - host
      - fx
      - bootstrap
      - content
      - provider
      - types
      - httpapi
  testsupport:
    mayDependOn:
      - app
      - content
      - types
